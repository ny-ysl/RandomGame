using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class MainUIController : MonoBehaviour
{
    /*
    GameObject Panel_Btn;
    GameObject Panel_Help;
    GameObject Panel_Setting;
    AudioSource Audio_Bg;
    Slider musicSd;

    void ClickStartBtn()
    {
        Debug.Log("ClickStartGame unity");
    }

    void ClickHelpBtn()
    {
        void CloseView()
        {
            Panel_Help.SetActive(false);
        }
        Debug.Log("ClickHelpGame unity");
        Panel_Help.SetActive(true);
        GameObject btnCloseObj = GameObject.Find("Panel_Help/Button_Close");
        Button close_btn = (Button)btnCloseObj.GetComponent<Button>();
        close_btn.onClick.AddListener(CloseView);
    }

    public void con_sound()
    {
        Audio_Bg.volume = musicSd.value;
    }


    void ClickSettingBtn()
    {
        void CloseView()
        {
            Panel_Setting.SetActive(false);
        }
        Debug.Log("ClickSettingGame unity");
        Panel_Setting.SetActive(true);
        GameObject btnCloseObj = GameObject.Find("Panel_Setting/Button_Close");
        Button close_btn = (Button)btnCloseObj.GetComponent<Button>();
        close_btn.onClick.AddListener(CloseView);

        


    }

    void InitBtn()
    {
        GameObject btnStartObj = GameObject.Find("Panel_Btn/Button_Start");
        Button start_btn = (Button)btnStartObj.GetComponent<Button>();
        start_btn.onClick.AddListener(ClickStartBtn);

        GameObject btnHelpObj = GameObject.Find("Panel_Btn/Button_Help");
        Button help_btn = (Button)btnHelpObj.GetComponent<Button>();
        help_btn.onClick.AddListener(ClickHelpBtn);

        GameObject btnSettingObj = GameObject.Find("Panel_Btn/Button_Setting");
        Button setting_btn = (Button)btnSettingObj.GetComponent<Button>();
        setting_btn.onClick.AddListener(ClickSettingBtn);
    }

    void Start()
    {
        Panel_Btn = GameObject.Find("Panel_Btn");
        Panel_Help = GameObject.Find("Panel_Help");
        Panel_Setting = GameObject.Find("Panel_Setting");
       // Audio_Bg = GameObject.Find("Audio_Bg");

        Panel_Btn.SetActive(true);
        Panel_Help.SetActive(false);
        Panel_Setting.SetActive(false);
        //Audio_Bg.SetActive(true);

        InitBtn();

        Debug.Log("======================");
    }

    // Update is called once per frame
    void Update()
    {
        
    }*/
}
